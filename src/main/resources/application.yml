spring:
  application:
    name: goormone_1  # 원래) sample-project

  datasource:
    url: jdbc:postgresql://localhost:5432/sample
    driver-class-name: org.postgresql.Driver
    username: user
    password: password

  jpa:
    #    hibernate.ddl-auto:  create
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        highlight_sql: true

  sql:
    init:
      mode: always